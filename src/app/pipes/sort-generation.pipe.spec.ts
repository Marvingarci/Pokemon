import { SortGenerationPipe } from './sort-generation.pipe';

describe('SortGenerationPipe', () => {
  it('create an instance', () => {
    const pipe = new SortGenerationPipe();
    expect(pipe).toBeTruthy();
  });
});
